<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Country</title>
    <style>
        * {
            font-family: Arial, Helvetica, sans-serif;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            width: 90%;
            max-width: 1200px;
            align-items: center;
            justify-items: center;
        }

        .country_card {
            max-width: fit-content;

            border: 1px solid black;
        }

        .country_card img {
            width: 100%;
            height: 70%;

        }
        .country_card h3, .country_card p {
            text-align: left; 
            margin-left: 15px;
    }


    </style>
</head>
<body>
    <h1 style="align-self: left;">List Of Countries</h1>
    <div class="container">
        <% data.forEach((item)=>{ %>
            <div class="country_card">
                <img src="http://10.0.15.21:8000<%= item.flag %>" alt="">
                <h3><%= item.name %></h3>
                <p>Capital: <%= item.capital %></p>
                <p>Region: <%= item.region %></p>
                <p>Population: <%= item.population %></p>
                <p>Area: <%= item.area %></p>
                <p>CIOC: <%= item.cioc %></p>
            </div>
        <% }); %>  
    </div>

</body>
</html>
